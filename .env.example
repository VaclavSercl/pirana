# PIRANA development environment template.
# Production systemd deployment must use unit-private Bitfinex credentials
# under /etc/pirana/credentials instead of storing exchange keys here.
#
# To use this file for local DEVELOPMENT only, opt in explicitly:
PIRANA_LOAD_DOTENV=1
BITFINEX_API_KEY=
BITFINEX_API_SECRET=
PIRANA_TESTNET=false

# Risk source of truth:
# - hard ceilings: crates/pirana-core/src/constants.rs
# - active strategy: strategy.toml
# - calibrated/persisted state: /opt/caslav/risk/risk_state.json
# Historical MAX_* environment overrides are intentionally ignored.

# Infrastructure — safe defaults bind locally; reverse proxy/container may opt in
PIRANA_RUST_METRICS_PORT=9100
HEALTH_CHECK_PORT=8080
PIRANA_DASHBOARD_BIND=127.0.0.1
PIRANA_METRICS_BIND=127.0.0.1
PIRANA_EXPORTER_BIND=127.0.0.1
PIRANA_EXPORTER_PORT=9091
LOG_LEVEL=info
PIRANA_ENV=production

# Durable state (defaults shown)
PIRANA_ACCOUNTING_DB=/var/lib/pirana/accounting.sqlite3
PIRANA_ACCOUNTING_SNAPSHOT_PATH=/var/lib/pirana/accounting_snapshot.json
PIRANA_POSITION_SNAPSHOT_PATH=/var/lib/pirana/positions.json
CASLAV_RISK_STATE_PATH=/opt/caslav/risk/risk_state.json

# Grafana — intentionally blank so Docker Compose fails closed until configured
GRAFANA_PASSWORD=

# Telegram control/reporting (development only). Production services read
# /etc/pirana/telegram.env, a root-owned file separate from exchange credentials.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
