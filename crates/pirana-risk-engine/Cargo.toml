[package]
name = "pirana-risk-engine"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
pirana-core = { path = "../pirana-core" }
pirana-telemetry = { path = "../pirana-telemetry" }
serde = { workspace = true }
serde_json = { workspace = true }
chrono = { workspace = true }
tracing = { workspace = true }
thiserror = { workspace = true }
parking_lot = { workspace = true }
metrics = { workspace = true }

[dev-dependencies]
# Integracni test tests/mode_cycle.rs overuje cely cyklus rezimu
# pres governance + risk engine dohromady — deadlock 24. 8. vznikl
# teprve jejich slozenim, zadny unit test ho nemohl chytit.
pirana-signal-validator = { path = "../pirana-signal-validator" }
